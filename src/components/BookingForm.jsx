const BookingReservation = ({ formInput }) => {
  const title = "Reserve a table";

  return (
    <>
      <h2>{title}</h2>
      <input type="text" placeholder="Enter your name" />
    </>
  );
};

export default BookingReservation;
